  
name: Build And Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: WaveBeans/wavebeans/docker/gh-build@master
        with:
          args: ./gradlew test